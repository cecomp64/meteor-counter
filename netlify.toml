[build]
  publish = "."
  functions = "netlify/functions"

[dev]
  autoLaunch = false
  port = 8888
  functions = "netlify/functions"
  # Serve static files directly from publish directory
  staticServerPort = 8888

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
