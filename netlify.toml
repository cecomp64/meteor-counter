[build]
  command = "npm run build"
  publish = "."
  functions = "netlify/functions"

[dev]
  command = ""
  targetPort = 8888
  port = 8888
  publish = "."
  functions = "netlify/functions"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
