[build]
  publish = "."
  functions = "netlify/functions"

[dev]
  autoLaunch = false
  port = 8888
  publish = "."
  functions = "netlify/functions"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
